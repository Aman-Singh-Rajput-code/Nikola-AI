# Nikola AI — Architecture

This file is a placeholder pointer.

The full architecture design for Nikola AI — Clean Architecture layering, the complete
folder structure with per-file explanations, the AI Brain provider-agnostic abstraction,
end-to-end information flow, the Planner/Agent/Tool Registry/Brain communication contracts,
the Permission System design, and multi-agent/testing/scalability guidance — was authored as
a standalone architecture document during this project's design phase, prior to Sprint 1.

Action item for Sprint 1 wrap-up: paste that architecture document's full content into this
file (replacing this placeholder) before opening the first pull request, so `ARCHITECTURE.md`
is committed to version control alongside the skeleton it describes. Treat this exactly like
a TODO that should not survive past the first commit.
